import 'package:flutter/material.dart';

class IntensityControl extends StatelessWidget {
  const IntensityControl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
